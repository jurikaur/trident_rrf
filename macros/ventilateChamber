;ventilate Chamber
if exists(global.ventilateChamber)
  while global.ventilateChamber > 0
    M106 P3 S1
    set global.ventilateChamber = ((global.ventilateChamber*60000)-250)/60000
  G4 P250

  if global.ventilateChamber <= 0
    M106 P3 S0 ; stop exhaust