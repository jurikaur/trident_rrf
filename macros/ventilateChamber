;ventilate Chamber
if exists(global.ventilateChamber)
  var delay = global.ventilateChamber
  if (job.file.fileName = "") || (job.file.fileName=null)
    if global.ventilateChamber <= 0
      M106 P3 S0 ; stop exhaust
    ;    M98 P"0:/macros/Air filtration/Air filtration OFF"                          ; turn off air filtration fan
    else
      M106 P3 S1
      set global.ventilateChamber = (var.delay - (1/6))
  ;else
    ;abort "Print in progress"